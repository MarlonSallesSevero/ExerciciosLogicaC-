#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void){

float a,b, alt;

printf("informe a Base:");
scanf("%f",&b);
printf("informe a altura:");
scanf("%f",&alt);
a = (b * alt)/2;
printf("area = %.2f", a);

}
